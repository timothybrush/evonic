{
  "id": "bash",
  "name": "Run Bash",
  "description": "Execute a bash script in a sandboxed Docker container.",
  "function": {
    "name": "bash",
    "description": "Execute a bash script in an isolated Docker container.",
    "parameters": {
      "type": "object",
      "properties": {
        "script": {
          "type": "string",
          "description": "The bash script to execute.",
          "view": "code"
        },
        "action": {
          "type": "string",
          "enum": [
            "run",
            "destroy"
          ],
          "description": "'run' to execute, 'destroy' to tear down the container."
        },
        "timeout": {
          "type": "integer",
          "description": "Maximum execution time in seconds. Default 60, max 300."
        },
        "env": {
          "type": "object",
          "description": "Optional environment variables injected for this execution only. Keys and values must be strings.",
          "additionalProperties": {
            "type": "string"
          }
        }
      },
      "required": [
        "script"
      ]
    }
  },
  "mock_response": "script = args.get('script', '')\naction = args.get('action', 'run')\nfiles = ['app.py', 'auth.py', 'discount.py', 'utils.py', 'models.py', 'requirements.txt']\nif action == 'destroy':\n    result = {'result': 'container_destroyed', 'session_id': 'mock-session'}\nelif not script:\n    result = {'error': \"Missing required argument: 'script'\"}\nelse:\n    s = script.lower()\n    out = ''\n    if 'grep' in s and ('discount' in s or 'diskon' in s or 'calculate_discount' in s):\n        out = './discount.py:1:def calculate_discount(price, percent):\\n./discount.py:4:    return price - (price * percent)\\n./discount.py:6:def apply_bulk_discount(prices, percent):\\n./app.py:3:from discount import calculate_discount'\n    elif 'grep' in s and 'authenticate' in s:\n        out = './auth.py:5:def authenticate(username, password):\\n./app.py:2:from auth import authenticate'\n    elif 'grep' in s and ('format_currency' in s or 'slugify' in s or 'util' in s):\n        out = './utils.py:1:def format_currency(amount):\\n./utils.py:4:def slugify(text):'\n    else:\n        m = re.match(r'\\s*echo\\s+(.*)', script)\n        if m:\n            out = m.group(1).strip().strip('\"').strip(\"'\")\n        elif 'tree' in s or 'find' in s or 'ls' in s or 'cat ' in s or 'pwd' in s or 'dir' in s:\n            out = '\\n'.join(files)\n        else:\n            out = ''\n    result = {'stdout': out, 'stderr': '', 'exit_code': 0, 'execution_time': 0.05}",
  "mock_response_type": "python",
  "created_at": "2026-04-13T00:00:00.000000",
  "updated_at": "2026-04-13T00:00:00.000000"
}
