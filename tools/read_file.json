{
  "id": "read_file",
  "name": "Read file",
  "description": "Reads the entire content of a text file.",
  "function": {
    "name": "read_file",
    "description": "Read a text file with 1-based line numbers. Use offset for pagination.",
    "parameters": {
      "properties": {
        "file_path": {
          "description": "Path to the text file.",
          "type": "string"
        },
        "offset": {
          "description": "1-based line number to start reading from (default: 1).",
          "type": "integer"
        }
      },
      "required": [
        "file_path"
      ],
      "type": "object"
    }
  },
  "mock_response": "mock_files = {\"example.txt\": \"This is the first line of the file.\\nThis is the second line.\\nIt demonstrates the requested format.\\nEach line is prefixed by its number.\\nA colon separates the number from the content.\\nThis makes it easy to identify lines.\\nIt is much faster to read than a JSON array.\\nYou can use these numbers for patching.\\nAlmost at the end of the example.\\nThis is the tenth line.\", \"app.py\": \"from flask import Flask, request\\nfrom auth import authenticate\\nfrom discount import calculate_discount\\n\\napp = Flask(__name__)\\n\\n@app.route(\\\"/login\\\", methods=[\\\"POST\\\"])\\ndef login():\\n    data = request.get_json()\\n    if authenticate(data[\\\"username\\\"], data[\\\"password\\\"]):\\n        return {\\\"status\\\": \\\"ok\\\"}\\n    return {\\\"status\\\": \\\"unauthorized\\\"}, 401\\n\\nif __name__ == \\\"__main__\\\":\\n    app.run(debug=True)\", \"auth.py\": \"import hashlib\\n\\nUSERS = {\\\"admin\\\": \\\"8c6976e5b5410415bde908bd4dee15dfb167a9c873fc4bb8a81f6f2ab448a918\\\"}\\n\\ndef authenticate(username, password):\\n    hashed = hashlib.sha256(password.encode()).hexdigest()\\n    return USERS.get(username) == hashed\", \"discount.py\": \"def calculate_discount(price, percent):\\n    # Menghitung harga akhir setelah diskon persen.\\n    # BUG: percent dalam satuan persen sehingga harus dibagi 100.\\n    return price - (price * percent)\\n\\ndef apply_bulk_discount(prices, percent):\\n    return [calculate_discount(p, percent) for p in prices]\", \"utils.py\": \"def format_currency(amount):\\n    return \\\"Rp \\\" + format(amount, \\\",.0f\\\").replace(\\\",\\\", \\\".\\\")\\n\\ndef slugify(text):\\n    return text.lower().strip().replace(\\\" \\\", \\\"-\\\")\", \"models.py\": \"class Product:\\n    def __init__(self, name, price):\\n        self.name = name\\n        self.price = price\"}\nfile_path = args.get('file_path', '')\nmatched = None\nfor name in mock_files:\n    if file_path == name or file_path.endswith('/' + name):\n        matched = name\n        break\nif matched is None:\n    result = {'error': 'File not found: ' + file_path}\nelse:\n    lines = mock_files[matched].split('\\n')\n    formatted = '\\n'.join('{}: {}'.format(i + 1, c) for i, c in enumerate(lines))\n    result = {'result': formatted}",
  "mock_response_type": "python",
  "created_at": "2026-04-12T14:28:48.414484",
  "updated_at": "2026-04-21T00:00:00.000000"
}
