{
  "id": "str_replace",
  "name": "String Replace",
  "description": "Replace an exact string in a file \u2014 no line numbers needed, more reliable than patch for simple edits.",
  "function": {
    "name": "str_replace",
    "description": "Replace exact string in a file. Unlike patch, no line numbers needed.",
    "parameters": {
      "type": "object",
      "properties": {
        "file_path": {
          "type": "string",
          "description": "Absolute or relative path to the file to edit."
        },
        "old_str": {
          "type": "string",
          "description": "Exact text to find and replace. Include surrounding context for unambiguous match."
        },
        "new_str": {
          "type": "string",
          "description": "The replacement string. Use an empty string to delete old_str."
        },
        "count": {
          "type": "integer",
          "description": "Number of replacements to make (default 1). The file must contain exactly this many occurrences of old_str, otherwise an error is returned.",
          "default": 1
        }
      },
      "required": [
        "file_path",
        "old_str",
        "new_str"
      ]
    }
  },
  "mock_response": "old_str = args.get('old_str', '')\nnew_str = args.get('new_str')\ncount_val = args.get('count', 1)\nif not old_str:\n    result = {'error': \"'old_str' must not be empty\"}\nelif new_str is None:\n    result = {'error': \"Missing required argument: 'new_str'\"}\nelse:\n    result = {'result': 'success', 'replacements': count_val}",
  "mock_response_type": "python",
  "system_prompt": "## str_replace \u2014 Usage Rules\n\n**Prefer str_replace over patch for simple edits.** It is more reliable because it does not depend on line numbers.\n\n### When to use str_replace vs patch\n- `str_replace`: replacing a function body, changing a value, fixing a line \u2014 anything where you can identify the text uniquely\n- `patch`: inserting a new block of code with no unique surrounding text, or making many changes across a file in one shot\n\n### Workflow\n1. `read_file` the target file to get the current content\n2. Copy the exact text you want to replace into `old_str` (including 1-2 lines of context)\n3. Write the replacement in `new_str`\n4. If the error says 'not found', re-read the file \u2014 the content may have changed\n5. If the error says 'found N times', make `old_str` more specific\n\n### Common mistakes to avoid\n- Do **not** guess `old_str` from memory \u2014 always copy it from the file\n- Do **not** use partial matches; include enough context to be unambiguous",
  "created_at": "2026-04-20T00:00:00.000000",
  "updated_at": "2026-04-20T00:00:00.000000"
}
